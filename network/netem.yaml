apiVersion: chaos-mesh.org/v1alpha1
kind: NetworkChaos
metadata:
  name: network-emulation
spec:
  action: netem
  mode: all
  selector:
    namespaces:
      - dev-stable
    labelSelectors:
      'app': 'dev-stable'
    fieldSelectors:
      'metadata.name': 'producer-pod-<id>'
  loss:
    loss: '30'
    correlation: '30'